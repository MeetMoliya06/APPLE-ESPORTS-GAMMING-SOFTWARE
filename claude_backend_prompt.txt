You are Claude Code. You have been invoked by Antigravity (the orchestrator agent) to build the backend infrastructure for the new Decentralized LAN Offline Architecture for AppleEsportsErp.

Please implement the following features in the C# .NET Backend (`AppleEsportsErp.Api` and `AppleEsportsErp.Core`):

1. **Emergency Offline Token Generation:**
   - Create an endpoint `POST /api/auth/emergency-token`.
   - It should generate a highly secure, long-lived JWT token (or similar cryptographic token) that embeds the user's role (e.g., Operator). 
   - This token is meant to be stored in the browser's IndexedDB protected by a 4-digit PIN, so just return the token string.

2. **Decentralized Sync Endpoints:**
   - Create `POST /api/sync/offline-session` which accepts session telemetry from an individual Gaming PC (e.g., `PcId`, `DurationSeconds`, `OfflineStartTime`).
   - Create `POST /api/sync/offline-billing` which accepts offline cash transactions from the Operator's PC.

Do NOT build the frontend React components. Antigravity is building those. Only modify the C# backend.
Please implement these controllers/endpoints and update the DbContext if necessary.
